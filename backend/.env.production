# ===================================================
# PRODUCTION ENVIRONMENT VARIABLES - RENDER DEPLOYMENT
# ===================================================
# Copy these to Render's "Environment" settings and update values

# Server Configuration
PORT=5001
NODE_ENV=production

# Database - MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://kushalpatel02709_db_user:5AjqTYWE9nFwfvE9@cluster0.1dzq1kb.mongodb.net/chat_db?retryWrites=true&w=majority&appName=Cluster0

# JWT Secret - Keep this secure! Generate a new strong secret for production
# Generate with: openssl rand -base64 32
JWT_SECRET=YOUR_PRODUCTION_JWT_SECRET_HERE

# Frontend URLs for CORS
# Update THIS to your actual frontend Render URL
FRONTEND_URL=https://gyansetu-be4p.onrender.com

# Additional frontend URLs (optional, comma-separated)
# Example: FRONTEND_URLS=https://staging-frontend.onrender.com,https://custom-domain.com
FRONTEND_URLS=

# Cloudinary Configuration (for image uploads)
CLOUDINARY_CLOUD_NAME=dpq20o63o
CLOUDINARY_API_KEY=595454565673335
CLOUDINARY_API_SECRET=aEVlqA0W7b5c4Mq3BEWwxF7vEYY

# Google OAuth (optional, only if using OAuth)
GOOGLE_CLIENT_ID=YOUR_GOOGLE_CLIENT_ID_HERE
GOOGLE_CLIENT_SECRET=YOUR_GOOGLE_CLIENT_SECRET_HERE
GOOGLE_REDIRECT_URI=https://gyansetu-n28h.onrender.com/api/auth/google/callback

# ===================================================
# DEPLOYMENT INSTRUCTIONS FOR RENDER
# ===================================================
#
# 1. Push this file to your GitHub repo (DO NOT commit sensitive secrets)
# 2. Go to your Render backend service settings
# 3. Navigate to: Settings → Environment
# 4. Click "Add Environment Variable" and enter each variable
# 5. For secrets, use Render's built-in secret management
# 6. Restart the service after adding variables
#
# Important Security Notes:
# - Never commit real secrets to GitHub
# - Use Render's secret management or GitHub Secrets
# - Change JWT_SECRET in production (don't use the dev one)
# - Regenerate Google OAuth keys for production
#
# ===================================================
